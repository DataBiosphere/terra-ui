const ConfigDetails = ({
  name, namespace, createDate, incompatible,
  payloadObject: {
    methodConfigVersion,
    inputs, outputs, rootEntityType,
    methodRepoMethod: { methodName, methodNamespace, methodVersion }
  }
}) => {

}

export default ConfigDetails
