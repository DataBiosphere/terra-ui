
import ButtonBar from 'src/components/ButtonBar'
import { h } from 'react-hyperscript-helpers'
import { CodeBlock, Playground } from 'src/pages/CodeBlock'

# Button Bar

This is **markdown** with <span style={{color: "red"}}>JSX</span>: MDX!

# Component: ButtonBar

The `ButtonBar` allows you to set the `onOk` event

<CodeBlock scopes={{ButtonBar}} live={true}>{`
  () => h(ButtonBar, {onOk: () => console.log('Clicked!')})
`}</CodeBlock>


<CodeBlock scopes={{ButtonBar}} live={false}>{`
  () => h(ButtonBar, {onOk: () => console.log('Clicked!')})
`}</CodeBlock>