import React from 'react';
import { TerraHexagonsAnimationOverlay } from 'src/branding/TerraHexagonsAnimation';

export const CreatingWorkspaceMessage = (): React.ReactNode => {
  return <TerraHexagonsAnimationOverlay message='Creating and provisioning your workspace.' />;
};
