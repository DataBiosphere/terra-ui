/*
 * This file is serving as a stub test while we have no other unit tests.
 * It ensures that our test setup remains intact in the interim.
 */
describe('stub', () => {
  it('exists', () => {
    expect(true).toBeTruthy()
  })
})
